# src/ is a regenerable artifact; if you don't want it tracked, uncomment the next line
# src/**
# PCB/schematics/firmware inside hardware_build/ cannot be regenerated from plain text → always keep under version control (do not ignore)

# Build and dependencies
node_modules/
dist/
build/
__pycache__/
*.pyc
.venv/
venv/

# Firmware/EDA temporary files
*.o
*.elf
*.hex.bak
*-backups/
*.kicad_prl
fp-info-cache

# Secrets (never commit to version control) — see ops/environment.md
.env
.env.*
!.env.example
*.key
*.pem
secrets/

# Operating system/editor
.DS_Store
Thumbs.db
.vscode/
.idea/
